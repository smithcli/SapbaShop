const mongoose = require('mongoose');

const userSchema = new mongoose.Schema({
  store:
  role:
  firstName:
  lastName:
  email:
  photo:
  active:
  password:
  passwordConfirm:
  passwordResetToken:
  passwordResetDate: 
})
